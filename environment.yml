name: molpalx

channels:
  - conda-forge

dependencies:
  - nvidia::cudatoolkit # for cuda >= 11
  # - pytorch::cudatoolkit # for cuda < 11
  # - pytorch::cpuonly
  - python=3.8
  - pytorch::pytorch=1.13
  - pip
  - openbabel=3.1.1
  - rdkit=2021.03.3 
