name: molpal

channels:
  - conda-forge
  - defaults

dependencies:
  - cudatoolkit=11.3
  - python=3.8
  - pytorch::pytorch >=1.9
  - rdkit
  - pip
  - pip:
    - configargparse
    - h5py
    - numpy
    - ray >= 1.7
    - ray[tune]
    - pytorch-lightning
    - scikit-learn
    - tensorflow
    - tensorflow-addons
    - tqdm
